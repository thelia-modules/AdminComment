{# AdminComment module configuration page (fragment rendered by the module.configuration hook) #}
<div class="card mb-3">
    <div class="card-header">
        <h2 class="card-title h5 mb-0">{{ 'Admin Comments'|trans({}, 'admincomment') }}</h2>
    </div>
    <div class="card-body">
        <p class="mb-0">
            {{ 'This module lets administrators add private comments on catalog and order pages. There is no configuration: a "Comment" tab and an inline comment widget are automatically added to the edit pages of products, categories, contents, folders, customers, orders and coupons.'|trans({}, 'admincomment') }}
        </p>
    </div>
</div>
